# iBooks 数字资源售卖平台 - 项目待办事项清单（历史快照）

> 基于 2026-04-27 项目代码审计整理，对照 `ibooks.md` 需求文档逐项核查。

> **历史快照说明（2026-09-10）**：下方“一”至“十”章节保留的是 2026-04-27 审计现场，未勾选项不代表当前仍缺失，不应继续作为实施清单。当前状态以本节、`DEV_PLAN.md` 和实际测试结果为准。

## 当前真实状态（2026-09-12 复核）

本次核对实现和文档，并重新通过前端 Jest `33 suites / 104 tests`、TypeScript、ESLint、后端 Ruff 和 `pip check`。以下完整验收证据来自 2026-09-10；后端全量覆盖率、构建、迁移、浏览器及漏洞审计未在本次重跑。最新进度与远程归档目标见 `DEV_PLAN.md` 的 2026-09-12 记录。

### 已完成并有验证证据

- [x] 生产配置校验、schema bootstrap、审计落库、批量上传安全已修复。
- [x] 搜索过滤草稿、爬虫默认草稿且不覆盖人工发布状态已修复。
- [x] 爬虫批量查重、数据库来源唯一约束、Redis 跨进程租约与真实运行状态恢复已完成。
- [x] moderator/admin 权限分层、订单并发防重复扣币、邮件一致性已修复。
- [x] `SEO_AUTO_GENERATE` 已接入启动生命周期，只生成本地 SEO 文件且不会隐式外部提交。
- [x] 资源交付边界已收敛：`GET /resources/{slug}/access` 只返回 `has_access`，`POST /download` 才返回交付字段并原子计数。
- [x] 公开设置仅返回白名单展示键的 `key/value`，后台分类、说明、更新时间与更新人不会泄露。
- [x] Access/refresh token 分离轮换及多标签页竞态已修复，旧请求不会清除其他标签页的新凭据。
- [x] `POST /seo/generate-all` 等待真实生成结果，成功返回文件清单，部分失败返回安全的成功/失败明细。
- [x] FAQ、联系方式、分类与分类资源已区分加载失败和真实空态，并提供明确错误提示/重试路径。
- [x] `/admin/users` 已统一为 `items/total/page/page_size/pages` 当前分页对象契约。
- [x] 后端生产依赖与 `requirements-dev.txt` 质量门禁依赖已拆分，依赖漏洞审计无已知漏洞。
- [x] 前端 Next.js `15.5.25`，Jest `33 suites / 104 tests passed`，TypeScript、ESLint、完整生产构建与 `npm audit` 通过，`npm audit` 为 0 已知漏洞。
- [x] 10 个公开路由分别以桌面与 375px 视口验收，共 20 组；均 HTTP 200，`brokenImages`、控制台错误、失败响应为 0，且 `scrollWidth === innerWidth`。
- [x] 浏览器验收中已修复历史封面与 `/og-image`，并停用 seed 数据中的无效二维码。
- [x] 后端最新全量质量门禁 `292 passed`，总覆盖率 `72.42%`（约 `72%`，门槛 70%），Ruff、`pip check` 与 `pip-audit` 通过，依赖审计为 0 已知漏洞。
- [x] Alembic 全新数据库 `upgrade -> downgrade -1 -> upgrade` 与 `alembic check` 通过，paid 订单部分唯一索引与爬虫来源唯一约束已验证。

### 当前真实剩余项与外部阻塞

- [ ] 支付宝沙箱：缺少/尚未使用真实沙箱凭据完成充值跳转、异步回调和到账验证。
- [ ] 百度与 SMTP：需要真实凭据和公网环境完成 URL 推送、邮件投递联调。
- [ ] Docker 全容器验收：尚未使用必需的显式密钥与公网 URL 完成整套生产式服务及 Grafana 联动验收；Docker Hub 拉取超时是 2026-09-10 记录的外部环境问题，本次未重测。
- [ ] Google Search Console / 360 自动提交：仅在产品仍要求时继续实现并做真实平台联调。
- [ ] 运行边界：多 worker crawler scheduler 仍有重复轮询开销；生产库若已有重复来源数据，来源唯一约束迁移前需人工清理。

> 最新测试口径：隔离测试数据库与 Redis 后执行完整质量门禁，结果为 `292 passed`、总覆盖率 `72.42%`，满足 70% 发布门槛；Ruff、`pip check`、`pip-audit` 及 Alembic 往返/一致性检查均通过。前端为 `33 suites / 104 tests passed`，TypeScript、ESLint、完整生产构建与 `npm audit` 通过且审计为 0 已知漏洞；10 个公开路由的桌面/375px 共 20 组浏览器回归全部通过。

---

## 一、项目总体完成度评估

| 模块 | 声称完成度 | 实际完成度 | 说明 |
|------|-----------|-----------|------|
| 后端 API | 100% | ~75% | API 端点齐全，但缺少支付、邮件、迁移文件等关键基础设施 |
| 前端展示站 | 90% | ~65% | 页面结构完整，但多个页面使用 mock 数据，核心购买流程缺失 |
| 管理后台 | 已声称完成 | ~60% | 多个管理页面未对接真实 API，设置页面无法保存 |
| 监控系统 | 已声称完成 | ~50% | Prometheus 配置存在，Grafana/Promtail 配置文件缺失 |
| 测试覆盖 | 未说明 | ~5% | 仅 2 个后端认证测试，前端测试为零 |

---

## 二、严重问题（P0 - 必须立即修复）

### 2.1 管理员权限校验缺失
- **现状**: 前台登录和管理端登录使用同一个 `/api/v1/auth/login` 端点，前端未校验用户角色
- **风险**: 任何注册用户拿到 token 后可直接访问 `/admin/*` 页面
- **待办**:
  - [ ] 后端：增加 `/api/v1/auth/admin/login` 端点，或在登录响应中返回角色信息
  - [ ] 前端：`useAdminAuth` hook 增加角色校验，非 admin 用户重定向到前台
  - [ ] 前端：admin 路由组增加角色守卫中间件

### 2.2 `get_optional_user` 异步调用 Bug
- **位置**: `backend/app/dependencies.py`
- **现状**: `get_optional_user` 同步调用了 `async` 函数 `get_current_user`，运行时会返回协程对象而非用户数据
- **待办**:
  - [ ] 修复为 `await get_current_user(credentials, db)`

### 2.3 数据库迁移文件缺失
- **现状**: `alembic/` 目录仅有 `env.py` 和 `script.py.mako`，无任何迁移脚本
- **风险**: 无法通过 `alembic upgrade head` 初始化数据库，生产部署受阻
- **待办**:
  - [ ] 生成初始迁移文件：`alembic revision --autogenerate -m "initial_schema"`
  - [ ] 验证迁移脚本可正确执行 upgrade/downgrade

---

## 三、核心功能缺失（P1 - 影响产品可用性）

### 3.1 支付/订单系统（完全缺失）
> 对照需求文档：资源有 `price` 字段，但无购买流程

- **待办**:
  - [ ] 后端：创建 `Order` 模型（订单号、用户ID、资源ID、金额、状态、支付方式、支付时间）
  - [ ] 后端：创建 `Payment` 模型（交易流水号、支付渠道、回调数据）
  - [ ] 后端：实现订单 CRUD API (`/api/v1/orders`)
  - [ ] 后端：集成支付网关（微信支付 / 支付宝 二选一先做）
  - [ ] 后端：支付回调通知处理
  - [ ] 前端：资源详情页增加购买按钮和支付流程
  - [ ] 前端：用户订单列表页面
  - [ ] 前端：管理后台订单管理对接真实 API（当前为 mock 数据）

### 3.2 云盘链接交付机制（未实现）
> 对照需求文档："商品可展示云盘链接（购买后）"

- **现状**: 资源详情页"获取资源"按钮无点击处理逻辑
- **待办**:
  - [ ] 定义交付逻辑：免费资源直接展示链接 / 付费资源购买后展示
  - [ ] 后端：增加获取云盘链接接口（校验购买状态）
  - [ ] 前端：资源详情页根据购买状态展示/隐藏云盘链接和提取码

### 3.3 邮件系统（未实现）
> 对照需求文档："邮箱注册登录"

- **现状**: User 模型有 `email_verification_token` 字段，但无发件逻辑
- **待办**:
  - [ ] 后端：实现邮件发送工具（SMTP / 第三方邮件服务）
  - [ ] 后端：注册后发送验证邮件
  - [ ] 后端：增加邮箱验证接口
  - [ ] 后端：密码重置邮件流程
  - [ ] 前端：邮箱验证提示页面
  - [ ] 前端：忘记密码 / 重置密码页面

---

## 四、管理后台未完成项（P1）

### 4.1 联系方式管理 - API 未对接
- **位置**: `frontend/src/app/admin/contacts/page.tsx`
- **现状**: 代码中有 `// TODO: Implement update API` 和 `// TODO: Implement delete API`
- **待办**:
  - [ ] 对接更新联系方式 API（`toggleStatus` 函数）
  - [ ] 对接删除联系方式 API（`handleDelete` 函数）

### 4.2 用户管理 - 使用 Mock 数据
- **位置**: `frontend/src/app/admin/users/page.tsx`
- **现状**: 真实 API 调用被注释，使用 3 条硬编码数据
- **待办**:
  - [ ] 取消注释并对接 `api.admin.getUsers()` 真实 API
  - [ ] 对接用户状态切换 API
  - [ ] 对接用户角色修改 API

### 4.3 订单管理 - 完全 Mock
- **位置**: `frontend/src/app/admin/orders/page.tsx`
- **现状**: 页面提示"订单系统后端API正在开发中"
- **待办**:
  - [ ] 依赖 3.1 支付/订单系统完成后对接

### 4.4 系统设置 - 未对接后端
- **位置**: `frontend/src/app/admin/settings/page.tsx`
- **现状**: `handleSave` 使用 `setTimeout` 模拟保存，未调用任何 API
- **待办**:
  - [ ] 对接 `GET /api/v1/settings/grouped` 加载设置
  - [ ] 对接 `PUT /api/v1/settings/{key}` 保存设置
  - [ ] 对接 `POST /api/v1/settings/batch` 批量保存

### 4.5 仪表盘 - 部分 Mock
- **位置**: `frontend/src/app/admin/page.tsx`
- **现状**: 统计数据来自真实 API，但"最近资源"和"最近订单"为硬编码数据
- **待办**:
  - [ ] "最近资源"对接 resources API（按时间倒序取最新 5 条）
  - [ ] "最近订单"依赖订单系统完成后对接

---

## 五、监控与基础设施（P2）

### 5.1 监控配置文件缺失
- **现状**: `docker-compose.yml` 引用了不存在的配置文件
- **待办**:
  - [ ] 创建 `monitoring/grafana/provisioning/` 目录及 datasource/dashboard 配置
  - [ ] 创建 `monitoring/promtail/promtail-config.yml`
  - [ ] 创建预置 Grafana 仪表板（API 性能、系统资源）

### 5.2 Docker 配置端口冲突
- **现状**: README 中前端和 Grafana 都声称使用 3000 端口
- **待办**:
  - [ ] 确认并修正端口分配（建议 Grafana 用 3001）
  - [ ] 更新 README 端口说明

### 5.3 健康检查与自动重启
> 对照需求文档："容器健康检查"

- **待办**:
  - [ ] `docker-compose.yml` 为各服务添加 `healthcheck` 配置
  - [ ] 添加 `restart: unless-stopped` 策略

---

## 六、安全加固（P2）

### 6.1 用户注册安全
- **现状**: 注册接口完全开放，无任何限制
- **待办**:
  - [ ] 添加注册频率限制（rate limiting）
  - [ ] 添加验证码（可选）
  - [ ] 考虑是否需要管理员审批机制

### 6.2 JWT 安全增强
- **待办**:
  - [ ] 实现 token 刷新机制（当前 token 24h 过期后需重新登录）
  - [ ] 实现登出接口（token 黑名单 / Redis 存储）

### 6.3 前端安全
- **待办**:
  - [ ] 前端存储 token 方式评估（当前 localStorage，建议改为 httpOnly cookie）
  - [ ] 管理后台增加操作确认（批量删除等危险操作）

---

## 七、测试覆盖（P2）

### 7.1 后端测试
- **现状**: 仅 `tests/test_auth.py` 包含 2 个基础测试
- **待办**:
  - [ ] 资源 CRUD API 测试
  - [ ] 分类管理 API 测试
  - [ ] 搜索功能测试
  - [ ] 管理员权限 API 测试
  - [ ] 批量导入测试
  - [ ] SEO 生成器测试
  - [ ] 中间件测试（审计、性能）

### 7.2 前端测试
- **现状**: 零测试
- **待办**:
  - [ ] 核心组件单元测试（HeroSection、SearchBar、ResourceCard）
  - [ ] 页面集成测试
  - [ ] Admin 认证流程测试
  - [ ] API 交互 mock 测试

---

## 八、SEO 与用户体验优化（P3）

### 8.1 搜索引擎自动提交
> 对照需求文档："自动提交到百度、360、Google（可配置）"

- **待办**:
  - [ ] 实现百度站长平台 URL 推送
  - [ ] 实现 Google Search Console API 提交
  - [ ] 管理后台增加 SEO 提交状态查看

### 8.2 前端页面优化
- **待办**:
  - [ ] 分类页面联动资源筛选
  - [ ] 联系方式组件在前端页面的展示样式优化（需求文档要求：卡片/侧边栏/弹窗可配置）
  - [ ] FAQ 页面对接后端 API（确认是否已对接）
  - [ ] 移动端适配检查与修复

---

## 九、未来扩展（P4 - 按需规划）

以下为需求文档中提及的扩展能力，当前无需实现，但需考虑架构预留：

| 功能 | 说明 | 优先级 |
|------|------|--------|
| AI 自动生成商品介绍 | 调用 LLM 生成 SEO 文案 | 中 |
| AI 标签分类自动化 | 自动为资源打标签 | 低 |
| 向量搜索/相似推荐 | 基于 embedding 的推荐系统 | 低 |
| 订阅制会员 | 月付/年付会员体系 | 中 |
| 推广返佣 | 分销系统 | 低 |
| 多商家入驻 | 平台化 | 低 |
| 移动端 APP | React Native / Flutter | 低 |
| 自动 HTTPS | Let's Encrypt 集成 | 部署时处理 |
| 数据库自动备份 | pg_dump 定时任务 | 部署时处理 |

---

## 十、推荐开发优先级路线

```
阶段 1 - 修复基础（1~2 天）
  ├── 修复 get_optional_user 异步 bug
  ├── 生成 Alembic 迁移文件
  ├── 修复管理员权限校验
  └── 管理后台 Mock 数据替换为真实 API（联系方式、用户、设置、仪表盘）

阶段 2 - 核心业务闭环（3~5 天）
  ├── 云盘链接交付机制
  ├── 支付/订单系统（至少支持一种支付方式）
  └── 邮件系统（注册验证 + 密码重置）

阶段 3 - 质量保障（2~3 天）
  ├── 后端核心 API 测试覆盖
  ├── 前端关键组件测试
  ├── 安全加固（rate limiting、token 刷新）
  └── 监控配置补全

阶段 4 - 体验优化（1~2 天）
  ├── SEO 自动提交
  ├── 前端交互细节优化
  └── 移动端适配
```

---

*文档生成时间: 2026-04-27*
*基于代码审计自动生成，建议结合实际业务优先级调整顺序*
